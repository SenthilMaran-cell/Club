version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site deployment"
  artifacts:
    baseDirectory: .
    files:
      - "index.html"
      - "style.css"
      - "script.js"
      - "amplify.yml"
  cache:
    paths: []
